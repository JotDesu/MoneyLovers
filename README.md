# MoneyLovers
A simple web app for tracking monthly expenses from shopping receipts. Snap a photo of a receipt (e.g. from a convenience store) and it's auto-read via OCR (Tesseract.js) right in the browser, or add items manually. Automatic monthly summaries, no server/database — 100% HTML/JS.
